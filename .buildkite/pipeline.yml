steps:
  - label: ":bun: pi-cursor-rules check"
    agents:
      queue: "default"
    image: "ghcr.io/carteramesh/docker/ubuntu:latest"
    commands:
      - bun install --frozen-lockfile
      - bun run check
